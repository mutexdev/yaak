{
  "name": "@yaak/importer-yaak",
  "displayName": "APIDoctor Importer",
  "description": "Import data from APIDoctor export files",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "yaakcli build",
    "dev": "yaakcli dev",
    "lint":"tsc --noEmit && eslint . --ext .ts,.tsx",
    "test": "vitest --run tests"
  }
}
